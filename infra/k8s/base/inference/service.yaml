apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: mlops
spec:
  selector:
    app: inference-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
